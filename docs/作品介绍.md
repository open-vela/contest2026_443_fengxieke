# 火眼——电动自行车充电电气安全监测终端（作品介绍）

## 一、作品概述

**火眼** 是一款部署在小区充电区/楼道充电点的智能监测终端，基于 **openvela** 与 **兆易创新 GD32F470V-START（板载芯片 GD32F407）**。它实时采集充电回路的**电流、温度**状态，通过端侧算法判定过流/超温异常，触发**声光报警并联网上报**，并把报警事件留痕到上位机（JSONL 事件记录），从电气侧阻断电动自行车充电火灾（漏电监测为后续扩展项）。

## 二、解决的真实问题

电动自行车入户充电引发的火灾事故频发（电池热失控是主因）。传统视频识别侧重"看得见"；本作品从**电气参数**切入——**电流异常与温升是热失控最直接的前兆**，监测维度更本质、更可靠，且**端侧离线闭环**（断网仍可声光报警与断电保护，报警事件同步留痕到上位机）。

## 三、技术方案

### 1. 应用层（`app/fireeye/`）
- **滤波模块**：滑动平均 + 一阶低通，去除采样噪声；
- **阈值判定模块**：单阈值 + 趋势（线性回归斜率）+ 连续 N 点防抖；
- **状态机**：正常→预警→报警→断电，支持复位；
- **主程序**：10Hz 采样循环，融合电流/温度，驱动报警与上报。

### 2. 新硬件适配（`docs/adaptation_F407.md`）
- 将 openvela 移植到**官方未完整支持的 GD32F407**：补齐芯片头文件、引脚映射、内存映射、Flash 编程扇区与 CMake 分发；
- 芯片实测为 **GD32F407VK**：`FMC_SIZE=3072KB`（3MB Flash）、SRAM 192KB、主频 168MHz（实测寄存器读数为准）；
- 时钟：25MHz 晶振 → **168MHz**（PLLPSC=25/PLLN=336/PLLP=2/PLLQ=7，VCO 336MHz），修复了此前 PLL 预分频错误导致的启动死锁；
- 串口：**USART0 = PB6(TX)/PB7(RX)，115200-8-N-1**；板上 GD-Link 无虚拟串口，需外接 3.3V USB-TTL（JP6 第 16/13 脚）；
- 已实现 openvela 在 F407 上**编译通过、烧录运行**：固件包含火眼应用与 OLED 模块（`nm` 验证符号），
  RCU_PLL 常量实测 `0x07405419`；实机接 3.3V USB-TTL 后**串口正常输出 `NuttShell (NSH)` 与 `nsh>` 提示符**，
  火眼应用可运行；板级 LED 自检（PC6 心跳）用于快速判断系统是否启动。

### 3. 端到端验证
- PC 端算法原型 `app/fireeye_prototype/algorithm_prototype.py`：模拟电流/温度，验证"滤波→阈值→状态机→报警"，实测输出电流/温度各 **57 次报警**（该组模拟数据未触发 WARNING 段）+ 可视化曲线（`fireeye_algorithm_results.png`）。

## 四、目录结构

- `app/fireeye/` — 应用（filter/threshold/fsm/main + OLED 显示）
- `app/fireeye_prototype/` — PC 算法验证
- `docs/` — 开发协同指南、F407 适配记录、本介绍、提交清单
- `logs/liu369369/` — AI Coding 日志

## 五、运行方式

```bash
repo init -u https://github.com/open-vela/contest2026_443_fengxieke \
  -b dev-ai-contest-2026 -m contest2026_443_fengxieke.xml
repo sync -c -j8
source build/envsetup.sh
./build.sh vendor/gigadevice/boards/gd32f4/gd32f470v_start/configs/nsh --cmake -j8
```

烧录与串口调试见 `docs/adaptation_F407.md`。

## 六、亮点

- **电气侧防控**：监测热失控直接前兆（电流/温升），比视频识别更本质；
- **端侧离线闭环**：感知、判断、报警全程本地，断网可用；
- **新硬件适配**：将 openvela 迁移到官方未支持的新芯片（GD32F407），作为适配成果；
- **多模块算法**：滤波 + 阈值/趋势 + 防抖 + 四态状态机，工程化程度高。
